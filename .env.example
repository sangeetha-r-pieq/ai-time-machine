# Copy to .env.local and add your Groq API key
# In dev, Vite proxy uses GROQ_API_KEY (server-side, not bundled)
GROQ_API_KEY=your_groq_api_key_here

# Fallback for production builds (still visible in bundle — use a backend for production)
VITE_GROQ_API_KEY=your_groq_api_key_here
